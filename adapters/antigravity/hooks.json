{
  "hooks": {
    "beforeToolExecution": [
      {
        "matcher": "run_command",
        "command": "{{PWSH}} -NoProfile -ExecutionPolicy Bypass -File \"{{RAIZ_JSON}}\\.agents\\hooks\\guard-git.ps1\""
      }
    ]
  }
}
